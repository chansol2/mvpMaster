<!DOCTYPE html>
<html lang="ko">
  <!--Header-->
  <%- include('../common/header'); -%>
  <body>
    <%- include ('../navs/navbar-main') -%>
    <div class="container">
        <div class="row">
          <div class="col-12">
            <nav aria-label="breadcrumb">
              <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="#">침실가구</a></li>
                <li class="breadcrumb-item active" aria-current="page">
                  침대
                </li>
              </ol>
            </nav>
          </div>
        </div>
        <div class="row">
          <div class="col-6"><h3>일반침대</h3></div>
            <div class="d-flex justify-content-end px-0 col-6">
              <select
                class="form-select pe-1"
                style="width: 120px"
                aria-label="Default select example"
              >
                <option selected value="1">인기순</option>
                <option value="2">리뷰순</option>
                <option value="3">평점순</option>
                <option value="4">가격낮은순</option>
                <option value="5">가격높은순</option>
              </select>
            </div>
        </div>
        <div class="row">
          <div class="col-12 text-muted"><p>총 결과가 검색됐습니다.</p></div>
        </div>
        <div class="row">
          <% for (let i = 0; i < spl.length; i++) { %>
            <div class="col-lg-3 col-md-6 col-sm-6 col-xs-6 col-6 mb-5 pe-1 ps-2">
              <div class="product">
                <div class="thumb">
                  <a
                    href="/product/<%= spl[i].cate_id %>/detail?pid=<%= spl[i].id %>&o_url=<%= spl[i].org_url%>"
                    ><img class="image" src="<%= spl[i].img_url %>" alt="product img"
                  /></a>
                </div>
                <div class="content">
                  <p class="source"><%= spl[i].seller_nm %></p>
                  <p class="p-title"><%= spl[i].prd_nm %></p>
                  <div class="price"><%= spl[i].sales_price %>원</div>
                </div>
              </div>
            </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
    <!--Footer-->
    <%- include('../common/footer'); -%>
  </body>
</html>
